import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'co.fridgetracker.beta',
  appName: 'Fridge Tracker',
  webDir: 'dist/fridge-app',
  bundledWebRuntime: false
};

export default config;
